import 'package:flutter/material.dart';

class InProgressOrder extends StatelessWidget {
  const InProgressOrder({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('In Progress Order')),
    );
  }
}