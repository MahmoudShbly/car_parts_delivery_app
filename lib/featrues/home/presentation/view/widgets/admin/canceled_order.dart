import 'package:flutter/material.dart';

class CanceledOrder extends StatelessWidget {
  const CanceledOrder({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Canceled Order')),
    );
  }
}