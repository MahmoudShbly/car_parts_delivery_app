import 'package:flutter/material.dart';

class FinishedOrder extends StatelessWidget {
  const FinishedOrder({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Finished Order')),
    );
  }
}